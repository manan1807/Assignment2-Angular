import { RangepipeDirective } from './rangepipe.directive';

describe('RangepipeDirective', () => {
  it('should create an instance', () => {
    const directive = new RangepipeDirective();
    expect(directive).toBeTruthy();
  });
});
